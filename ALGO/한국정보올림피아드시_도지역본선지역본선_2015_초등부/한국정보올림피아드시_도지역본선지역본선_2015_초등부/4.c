#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>


int main() {
	int n;
	int ns[200001];
	int nc[200001];
	int a[200001];

	
	scanf("%d", &n);

	for (int i = 0; i < n; i++){
		scanf("%d %d", &ns[i], &nc[i]);
	}
	
	for (int i = 0, j = 0; i < n; i++){
		 
		
	}

	return 0;
}